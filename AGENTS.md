# AGENTS.md

本文件规定 Codex 或其他代码代理在本仓库内操作 HFSS 工程时必须遵守的约束。目标是保证仿真可追溯、结果可复核，并避免破坏原始模型。

## 1. 基本原则

- HFSS 负责电磁建模、网格划分和数值求解；Codex 负责脚本、实验组织、结果处理和文档维护。
- 不得仅凭脚本运行成功认定仿真有效。必须同时检查输出文件、求解日志和收敛信息。
- 不得把局部参数扫描描述为全局最优搜索。
- 不得从单个 case 或少量离散频点推导整个频段的普遍结论。
- 所有研究结论必须能追溯到具体模型、参数、运行目录和结果文件。

## 2. 模型与文件安全

- 禁止原地覆盖基准 `.aedt`、已接受的 `final/` 模型或原始测量数据。
- 每次仿真必须复制基准工程到独立运行目录后再修改。
- 修改变量前必须确认变量存在、单位一致、表达式依赖未被破坏。
- 参数更新工具若报告缺失变量，必须立即终止该 case。
- 大规模扫参前先运行一个基准 case 和一个参数变更 smoke test。
- `runs/`、`derived/` 和 HFSS 派生目录可不进入 Git，但最终候选必须保留必要的参数、Touchstone、日志和收敛摘要。

## 3. 实验记录

每个 case 至少保存：

- 基准工程标识或哈希；
- 参数与单位；
- HFSS/AEDT 版本；
- Design、Setup、Sweep 和端口配置；
- 启动时间、结束时间和退出状态；
- 求解日志；
- 导出的网络参数或场数据；
- 收敛状态；
- 评价指标及其计算区间；
- 生成该 case 的脚本或 manifest。

批量实验应使用稳定 UID，并生成机器可读的 manifest。失败 case 也必须记录，禁止静默删除。

## 4. 指标计算

- 优先在完整扫频数组上计算带宽、波纹、最差回波损耗和阻带抑制。
- 插值、平滑、截断和异常值处理必须显式记录。
- 主交叉点、谐振点或带边存在多个候选时，必须定义选择规则。
- Touchstone 端口映射必须显式检查；不得依赖未声明的网络互易性假设。
- 同一研究内所有 sweep 必须使用一致的指标定义，修改定义时应重新评估历史结果。

## 5. HFSS 执行

- Design、Setup、Sweep、端口数和安装路径不得在新增通用代码中写死；应通过配置或命令行传入。
- 并行任务数必须考虑 HFSS 许可证、CPU、内存和磁盘容量。
- HFSS 打开失败或求解失败可以有限次数重试，重试次数和最终错误必须进入日志。
- 导出文件存在不代表收敛通过；应解析 `.conv`、profile 或等效收敛信息。

## 6. 代码修改

- 保持现有命令兼容，除非 PR 明确说明破坏性变更及迁移方法。
- 通用逻辑与器件专用逻辑应分离：运行器、项目接口和结果存储属于通用层；参数、指标和物理约束属于适配层。
- 重复的 Touchstone 解析、指标计算和运行管理应集中到共享模块。
- 可脱离 HFSS 测试的模块必须增加单元测试；涉及 HFSS 的集成测试至少提供 dry-run 或 mock 路径。
- 修改完成后，先做静态检查和 dry-run，再执行真实 HFSS 仿真。

## 7. 科研表述

允许的表述：

- “在给定参数范围和采样策略下找到的最佳 case”；
- “物理假设驱动的参数探索”；
- “自动化仿真与后处理闭环”。

未经额外证据不得使用：

- “全局最优”；
- “证明该拓扑不可能实现”；
- “具有普适性”；
- “实验验证”，除非存在实际测量数据。

## 8. 提交与审查

- 对 `main` 的修改应通过分支和 Pull Request。
- PR 必须说明修改范围、验证方法、未验证部分和对现有工作流的影响。
- 文档变更不得暗示仓库已经实现尚处于规划阶段的功能。
